{
  "name": "repeatflow",
  "version": "0.1.0",
  "private": true,
  "description": "Foundation for a browser extension that learns repeated workflows with user control.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "fake-indexeddb": "^6.2.5"
  }
}
